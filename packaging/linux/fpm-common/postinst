#!/bin/sh
set -e
# Post-install hook: ensure executable bit & cache desktop entries if present
if [ -x /usr/bin/update-desktop-database ]; then
  /usr/bin/update-desktop-database -q || true
fi
exit 0
