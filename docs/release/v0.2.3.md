<!--
SPDX-License-Identifier: GPL-2.0-only

Project: Ecli
File: docs/release/v0.2.3.md
Website: https://www.ecli.io
Repository: https://github.com/SSobol77/ecli
PyPI: https://pypi.org/project/ecli-editor/0.0.1/

Copyright (c) 2026 Siergej Sobolewski

Licensed under the GNU General Public License version 2 only.
See the LICENSE file in the project root for full license text.
-->
# ECLI v0.2.3 Release Notes

ECLI v0.2.3 is a Panel Console Stabilization release. It keeps the
Services Foundation behavior intact while replacing the fragile F11 terminal
experiment with an ECLI-owned PySH Console Panel.

## Highlights

- Full PTY terminal emulator scope is rejected for ECLI 0.2.x.

- F11 opens or focuses the ECLI-owned PySH Console Panel.

- F12 focus switching between editor and panels is documented in F1 Help.

- The PySH backend uses subprocess argv form, not shell interpolation.

- Missing PySH backend diagnostics are deterministic.

- Console builtins are available for `cd`, `pwd`, `clear`, and `exit`.

- Command cancellation is available through Ctrl+C.

- Regression coverage exists for the PySH backend, console panel, keybindings,
  F1 Help discoverability, and forbidden PTY implementation paths.

## Non-Goals and Safety Boundaries

- No full terminal emulator.

- No VT parser.

- No raw interactive PySH embedded inside curses.

- No fullscreen terminal application support target.

- No PySH source migration.

- No monorepo conversion.

- No VMLab, QEMU, or QMP runtime scope.

- No privileged remediation.

## Validation

Release validation for this update uses:

```sh
uv run ruff check src tests
uv run pytest -q
uv run python scripts/check_runtime_imports.py
find src/ecli -maxdepth 4 -type f | sort | grep -E 'command_backends|terminal_screen.py|terminal_session.py' || true
```

The forbidden PTY path scan must produce no output.

## Release Artifact Gate

v0.2.3 is blocked unless the final GitHub Release contains exactly 21
ECLI-owned uploaded physical assets, one per canonical matrix entry. This exact
21 ECLI-owned asset rule applies to v0.2.3 and every future official ECLI
release.

The GitHub UI may display **Assets 23** because GitHub adds generated
`Source code (zip)` and `Source code (tar.gz)` archives automatically. Those two
source archives are not ECLI-owned uploaded artifacts and are not part of the
canonical 21 artifact contract entries.

Reduced official asset sets, workflow upload subsets, and deferred official
asset sets are forbidden. AppImage, openSUSE, Arch, Slackware, FreeBSD `.pkg`, FreeBSD
ports/chroot evidence, Nix flake evidence, NixOS package evidence, Docker helper
evidence, and workflow contract evidence are mandatory. Checksum sidecars are
CI/release verification evidence under `.checksums/`, not uploaded GitHub
Release assets.

Run the exact asset gate before publication:

```sh
uv run python scripts/verify_release_assets.py --version 0.2.3
```

Mandatory GitHub Release assets for v0.2.3:

```text
ecli_editor-0.2.3-py3-none-any.whl
ecli_editor-0.2.3.tar.gz
ecli_0.2.3_linux_x86_64.bin
ecli_0.2.3_linux_x86_64.tar.gz
ecli_0.2.3_linux_x86_64.deb
ecli_0.2.3_linux_x86_64.rpm
ecli_0.2.3_opensuse_x86_64.rpm
ecli_0.2.3_arch_x86_64.pkg.tar.zst
ecli_0.2.3_slackware_x86_64.txz
ecli_0.2.3_linux_x86_64.AppImage
ecli_0.2.3_freebsd_x86_64.pkg
ecli_0.2.3_freebsd_ports_chroot_evidence.tar.gz
ecli_0.2.3_macos_universal2_app_evidence.tar.gz
ecli_0.2.3_macos_universal2.dmg
ecli_0.2.3_win_x86_64.exe
ecli_0.2.3_win_x86_64_setup.exe
ecli_0.2.3_nix_flake_evidence.tar.gz
ecli_0.2.3_nixos_package_evidence.tar.gz
ecli_0.2.3_docker_deb_helper_evidence.tar.gz
ecli_0.2.3_docker_rpm_helper_evidence.tar.gz
ecli_0.2.3_workflow_contract_evidence.tar.gz
```

### Asset naming correction (numeric-prefix mistake)

During the v0.2.3 release the verifier and local staging contract briefly
required numeric public prefixes such as `01_pypi_wheel__ecli_editor-0.2.3-…`,
`16_windows_nsis__ecli_0.2.3_win_x86_64_setup.exe`, and
`21_workflow_contract__ecli_0.2.3_workflow_contract_evidence.tar.gz`. Those
prefixed names were uploaded once and were a mistake: numeric prefixes are
internal ordering only and must never appear in public GitHub Release asset
names. The public v0.2.3 GitHub Release was manually repaired after publication
and now carries the 21 clean names listed above, with no uploaded `.sha256`
files. From the next release onward, `scripts/verify_release_assets.py` rejects
any top-level file matching `^[0-9]{2}_.*__` so the mistake cannot recur.
